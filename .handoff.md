# ST-1 Handoff

## What changed

- `pubspec.yaml`: added `http: ^1.2.0`, `internet_connection_checker_plus: ^3.0.0`, dev `test: ^1.25.0`, `fake_async: ^1.3.0`. No `flutter`/`connectivity_plus`/`rxdart`.
- `lib/src/connection_health_state.dart`: 4-state enum with dartdoc on the enum + every variant (per CLAUDE.md §1).
- `lib/src/connection_health_monitor.dart`: class skeleton with full constructor (URL normalization, `_ownsClient`, broadcast `StreamController`, `_currentState = initial`), private fields, public method signatures (`start`/`stop`/`dispose`/`checkNow` throw `UnimplementedError('ST-2 will implement')`). `stream` + `currentState` getters work immediately. Full dartdoc per CLAUDE.md §Conventions: class-level backgrounding rule, `stop` vs `dispose` distinction, `checkNow` no-emit warning, `currentState` synchronous-read warning.
- `lib/neo_connection_health_monitor.dart`: barrel exporting BOTH `src/connection_health_state.dart` AND `src/connection_health_monitor.dart`. `grep -c "^export"` = 2.
- `lib/src/neo_connection_health_monitor_base.dart`: DELETED.
- `example/neo_connection_health_monitor_example.dart`: minimal smoke (to be replaced by ST-4).
- `test/neo_connection_health_monitor_test.dart`: skeleton smoke tests verifying public API surface (3 cases). Full 14-case suite is ST-3's job.

## Verification (all green)

- `dart pub get` — 52 deps resolved, no errors.
- `dart analyze --fatal-infos` — No issues found.
- `dart format --set-exit-if-changed .` — 0 changed.
- `grep -c "^export" lib/neo_connection_health_monitor.dart` — 2.
- `dart pub deps --no-dev | grep -c flutter` — 0.
- `test ! -e lib/src/neo_connection_health_monitor_base.dart` — OK deleted.

## Deviations from plan/CLAUDE.md

- `example/` and `test/` files seeded with minimal content for compilation. These are owned by ST-4 and ST-3 respectively — they should OVERWRITE these files. No deviation from spec.
- No `git push` — no remote configured on the parent branch. Local commit only. Reviewer to push the merged parent branch later.

## For the reviewer

- ST-2 picks up: replace `UnimplementedError('ST-2 will implement')` in `start`/`stop`/`dispose`/`checkNow` with real implementations per CLAUDE.md §3-7.
- `_composeUri` static helper at the bottom of the class is reusable; do not duplicate the normalization logic.
- Constructor already sets up everything ST-2 needs: `_uri`, `_httpClient`, `_internetChecker`, `_random`, `_controller`, `_ownsClient`. ST-2 only needs to add loop/check logic + lifecycle flag transitions.
