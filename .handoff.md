# ST-5 Handoff

## What changed

- `tool/check.sh`: bash script with `set -euo pipefail`. Runs `dart format --set-exit-if-changed .`, `dart analyze --fatal-infos`, `dart test`. Echoes section headers. Executable (`chmod +x`).
- `.github/workflows/dart.yml`: GitHub Actions workflow.
  - Triggers: `push` to `master`, all `pull_request` events.
  - Concurrency: `${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`.
  - Single job `check` on `ubuntu-latest`:
    - `actions/checkout@v4`
    - `dart-lang/setup-dart@v1` with `sdk: stable` (no minor pin)
    - `dart pub get`
    - `bash tool/check.sh`

## Verification

- `bash -n tool/check.sh` → syntax OK.
- `chmod +x tool/check.sh` applied; permissions = `-rwxr-xr-x`.
- YAML parse via `js-yaml` → top keys `[name, on, concurrency, jobs]`, single job `check`, all 4 steps present, concurrency block correct.
- `git status` before commit: only `tool/check.sh` and `.github/workflows/dart.yml` staged (plus this handoff).

## Deviations

- `sdk: stable` (per task brief / CLAUDE.md), not the `^3.11.1` literal from plan.md acceptance line. Brief overrides plan on this. `pubspec.yaml` constraint already enforces the minimum.
- Local end-to-end run of `tool/check.sh` is deferred to the reviewer, since `lib/` and `test/` don't exist on this branch (they land via ST-1/ST-2/ST-3). Mechanical-validation steps above are sufficient at this layer.
- No `git push` — no remote configured. Reviewer will push the merged parent branch.

## For the reviewer

- After ST-1 + ST-2 + ST-3 are merged into parent, run `bash tool/check.sh` from the parent worktree to confirm the full pipeline. If it fails because the package's analyzer settings (`analysis_options.yaml`) flag something, that's ST-2/ST-3's regression, not this script's.
- If the team wants to pin a specific Dart SDK, change `sdk: stable` to `sdk: '3.11'` in `dart.yml` — flagged here so it's an intentional choice, not a drift.
