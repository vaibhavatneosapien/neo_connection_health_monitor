# ST-4 handoff — example, README, CHANGELOG

Branch: `feat/neo-1651-st4-docs`
Parent: `feat/neo-1651-connection-health-monitor`

## What changed

- `example/neo_connection_health_monitor_example.dart` — replaced the ST-1 stub with a real end-to-end demo: constructs a monitor pointing at `https://api.neosapien.xyz`, subscribes to the stream, prints each emission, runs for 30 seconds, then cancels the subscription and disposes the monitor. Uses `// ignore: avoid_print` so the analyzer stays clean under `--fatal-infos`.
- `README.md` — full rewrite to match the ST-4 spec. Sections in required order: package intro, Install (`dart pub add` + path/git dep), Quick start (verbatim consumer snippet from CLAUDE.md §Quick reference), Caller responsibility — backgrounding (REQUIRED) with the verbatim "caller MUST call `stop()`" rule from CLAUDE.md §8 and the WHY (60 HTTP attempts/hour, App Store / Play Store battery reviews), Lifecycle snippet (full `WidgetsBindingObserver` block from CLAUDE.md §8 including the 5-state switch and `WidgetsBinding.instance.addObserver` wiring), States table mapping all 4 enum values to UI affordances, API table covering `stream` / `currentState` / `start` / `stop` / `dispose` / `checkNow` with the three required prominent notes (stop vs dispose, checkNow does NOT emit, currentState returns `initial` before first check), Why a pure-Dart package paragraph, Development pointing at `bash tool/check.sh`.
- `CHANGELOG.md` — overwritten with the `0.1.0` entry dated 2026-05-16 in Keep a Changelog format, covering ConnectionHealthMonitor, the four-state enum, server-first dual-tier check, split lifecycle, `checkNow` pure-probe semantics, broadcast stream with de-dupe, DI seams, and CI tooling. A Notes subsection re-states the caller-responsibility rule.

## Acceptance criteria verification

- `grep -c 'WidgetsBindingObserver' README.md` -> 3 (>= 1 required)
- `grep -c 'AppLifecycleState' README.md` -> 9 (>= 1 required)
- `grep -c 'MUST call' README.md` -> 1 (>= 1 required)
- CHANGELOG contains `## [0.1.0] - 2026-05-16`
- `dart format --set-exit-if-changed .` -> clean
- `dart analyze --fatal-infos example/` -> clean (no issues in my scope)

## Deviations

- The repo-level `dart analyze --fatal-infos` currently surfaces 5 errors in `lib/src/connection_health_monitor.dart` (`uri_does_not_exist` for `package:http` and `package:internet_connection_checker_plus`, plus the cascade of `undefined_class` / `undefined_method` from those). These are NOT caused by ST-4 changes — they exist because `pub get` has not been run in this worktree, and my sandbox lacks permission to invoke `dart pub get`. The errors live in files outside ST-4's scope. They will resolve as soon as the next agent in this worktree (or CI) runs `dart pub get`, and ST-2's full implementation of `ConnectionHealthMonitor` will be analyzed there. I scoped the analyzer call to `example/` to confirm my files are clean under `--fatal-infos`.
- `dart run example/neo_connection_health_monitor_example.dart` was NOT executed locally because (a) `dart pub get` is required first and is sandbox-blocked, and (b) the example calls `monitor.start()`, which still throws `UnimplementedError` until ST-2 lands (start/stop/dispose/checkNow are all stubs on the current branch's lib code). The example will run cleanly once ST-2 is merged into this branch's parent. The example itself is correct against the public API documented in `lib/src/connection_health_monitor.dart`.
- No `.handoff.md` for prior subtasks was found; this is the first handoff in this worktree.

## Files touched

- `example/neo_connection_health_monitor_example.dart`
- `README.md`
- `CHANGELOG.md`
- `.handoff.md` (this file)

## Files NOT touched (per scope constraints)

- `lib/`, `test/`, `pubspec.yaml`, `CLAUDE.md`, `plan.md`, `.gitignore`, `tool/`, `.github/`
